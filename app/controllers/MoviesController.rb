class MoviesController < ApplicationController
  def index
    # return all the movies

    # It looks like we established the connection
    # Next goal is do we create the migrations by hand, but I think we should probably import something from the db to define our structure
    # How do we actually get the info from the database we are using
    # We don't have a model
  end
end